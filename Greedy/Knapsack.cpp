#include <bits/stdc++.h>
using namespace std;
#define vi vector<int>
#define pii pair<int, int>
#define vii vector<pii>
#define rep(i, a, b) for (int i = a; i < b; i++)
#define ff first
#define ss second
#define setBits(x) builtin_popcount(x)
signed main()
{
    int n;
    cin >> n;
    vii a(n);
    rep(i, 0, n)
    {
        cin >> a[i].ff>>a[i].ss;
    }
    sort(a.begin(), a.end(), copmare);
    int ans = 0;
    r
}

// 10
// 1 2 5 10 20 50 100 200 500 200
// 54
