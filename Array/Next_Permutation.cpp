#include <iostream>
#include <queue>
#include <algorithm>
using namespace std;
void nextPer(int *arr, int n)
{
    int indx = -1;
}
int main()
{
    int n;
    int arr[] = {1, 2, 3, 6, 5, 4};
    n = sizeof(arr) / sizeof(int);
    nextPer(arr, n);
    for (int i = 0; i < n; i++)
    {
        cout << arr[i] << " ";
    }

    return 0;
}
